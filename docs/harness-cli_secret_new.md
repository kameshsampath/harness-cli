## harness-cli secret new

Creates a new secret.

```
harness-cli secret new [flags]
```

### Examples

```

  # Create new secret from file with default options
  main secret new --name foo --account-id <your account id> --project-id <project id> --file foo.txt
  # Create new secret from file with specific organization id
  main secret new --name foo --account-id <your account id> --project-id <project id> --file foo.txt --org-id=<orgid>
  # Create new secret from text 
  main secret new --name foo --account-id <your account id> --project-id <project id> --text foo --type=SecretText
  # Create new secret from text at account scope, default is project
  main secret new --name foo --account-id <your account id>  --text foo --type=SecretText --secret-scope="account"
  # Create new secret from text at org scope, default is project
  main secret new --name foo --account-id <your account id> --text foo --type=SecretText --secret-scope="org"

```

### Options

```
  -d, --description string         The description for the project.
  -m, --file string                The file content corresponding to secret. Used only when secret type is "SecretFile" or  "SSHKey" or "WinRmCredentials"
  -h, --help                       help for new
  -n, --name string                The name of the secret to create.
  -p, --project-id string          The project where the secret will be created.
  -s, --secret-manager-id string   The secret manager id to use. (default "harnessSecretManager")
      --secret-scope string        The secret scope. Valid value is one of "project", "org", "account" (default "project")
      --secret-type string         The secret type. Valid value is one of "SecretFile" "SecretText" "SSHKey" "WinRmCredentials" (default "SecretFile")
      --secret-value-type string   The secret value type, if the secret type is "SecretText". Valid value is one of "Inline", "Reference", "CustomSecretManagerValues" (default "Inline")
  -t, --tags stringArray           The tags to attach to the project, in the format of key:value e.g. foo:bar.
      --text string                The Secret text if the secret type is SecretText.
```

### Options inherited from parent commands

```
  -a, --account-id string   The harness account id.
  -k, --api-key string      The Harness API Key.
  -o, --org-id string       The organization id to use. (default "default")
  -v, --verbose string      The logging level to set (default "info")
```

### SEE ALSO

* [harness-cli secret](harness-cli_secret.md)	 - Group of commands to manipulate the connectors.

###### Auto generated by spf13/cobra on 8-Dec-2022
